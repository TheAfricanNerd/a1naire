import Section from "../Section";
import data from "./data";

const LimitedAdditions = () => {
  return (
    <>
      <Section data={data}></Section>
    </>
  );
};

export default LimitedAdditions;
